# de-ai-ify

**Tipo:** skill global ejecutable.

```bash
de-ai-ify clean --text "En el mundo de hoy, es importante destacar..." --dossier corralx
de-ai-ify clean --file caption.txt --brand path/to/brand.json
```

Respeta `banned_words` de `brand.json` + lista base anti-AI-slop.
