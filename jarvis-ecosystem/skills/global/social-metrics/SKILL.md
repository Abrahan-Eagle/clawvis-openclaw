# social-metrics

**Tipo:** skill global ejecutable.  
**Bin:** `skills/global/social-metrics/bin/social-metrics`  
**Estado:** v1

```bash
social-metrics pull --dossier corralx --dry-run
social-metrics pull --dossier corralx --live   # META_ACCESS_TOKEN + META_IG_USER_ID
social-metrics show --dossier corralx --period 2026-07
```

Salida: `state/metrics/<dossier>/<YYYY-MM>.json`.
