# competitor-intel

**Tipo:** skill global ejecutable.

```bash
competitor-intel ingest --dossier corralx --file /tmp/posts.json
competitor-intel viral-analyze --dossier corralx --dry-run
```

Plantilla: `templates/viral-posts.example.json`.
