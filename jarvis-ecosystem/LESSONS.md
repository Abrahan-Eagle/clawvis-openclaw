# Diario de Aprendizaje: Ecosistema Jarvis
> "El error es solo una iteración hacia la perfección."

## Registro de Lecciones Aprendidas

| ID | Fecha | Error | Causa Raíz | Acción Correctiva / Prevención |
|---|---|---|---|---|
| L001 | 2026-03-11 | Modelos `phi`/`tinyllama` fallaron | Falta de soporte para Tool-Calling | Migrado a `qwen2.5-coder:0.5b` que sí soporta herramientas en 4GB RAM. |
| L002 | 2026-03-11 | Bloqueo de archivos `.lock` | Ejecución concurrente accidental | Se implementó script de limpieza de locks antes de cada ejecución. |
| L003 | 2026-04-27 | Reutilizar código de Jarvis-MK37 (CC BY-NC 4.0) en el holding comercial | Licencia no comercial del repo de referencia | Análisis forense en `docs/FORENSE_JARVIS_MK37.md`; **sin copiar código** — patrones re-implementados (memory-store, core-prompt, skills, loop). |
| L004 | 2026-04-27 | Memoria solo en `MEMORY.md` | Texto largo no acota contexto de modelo | Añadido `memory.json` + skill `memory-store` con recorte y `format-prompt` por agente. |
| L005 | 2026-04-27 | Estilo y gates duplicados por agente | Cada `SOUL.md` redefinía límites | Creado `skills/global/core-prompt.md` y línea *Hereda* en todos los `SOUL.md`. |
| L006 | 2026-04-27 | Briefing sin señal externa barata | Depende de calendar/capability compleja | `morning-briefing` ahora hace `fetch` a Open-Meteo + puntero a `weather-report` y `youtube-transcript`. |
| L007 | 2026-04-27 | Navegación sin API sin control | Riesgo de exfil o dominios inseguros | Skill `browser-playwright` con allowlist + gate `AG-11`. |
| L008 | 2026-04-27 | Objetivos multi-paso ad hoc | Sin contrato reutilizable | Skills `planner` / `task-queue` / `executor` (dry-run) / `error-recovery` + `loop-orchestrator.yaml`. |
| L009 | 2026-04-27 | Agentes sin coordinacion ni log unificado | Cada agente operaba en su propio MEMORY.md sin ver al resto | Skills `activity-log` (jsonl event-source), `handoff` (contratos JSON validados por schema), `coordinator` (vista de jarvis) + automation `coordination-pulse.yaml`. Doc: `docs/COORDINACION_AGENTES.md`. |
| L010 | 2026-04-27 | RRSS de calidad solo via APIs pagas (Canva/RunwayML/etc.) | Costo y dependencia de la nube | Stack 100% libre/local: `brand-kit` + `image-render` (Pillow) + `image-ai-free` (Pollinations.ai) + `carousel-render` para carruseles; `tts-free` (edge-tts) + `subtitles` + `video-compose` (ffmpeg) para reels. Doc: `docs/CAROUSEL_PIPELINE_FREE.md`, `docs/REELS_TIKTOK_PIPELINE_FREE.md`. |
| L011 | 2026-04-27 | Demo E2E aborto por flags incorrectos | Skills internas (`carousel-render`, `video-compose`) llamaban `activity-log event` con `--task-id`/`--skill`/`--type`/`--message`/`--extra` cuando la API real es `--task`/`--agent`/`--kind`/`--note`/`--payload-file`. El demo replicaba el error. | Refactorizadas las llamadas internas para construir un `payload-file` con jq y usar los flags reales. Documentada la API correcta en `agents/marketing/SOUL.md` y `docs/DEMO_PIPELINE_RRSS.md`. |
| L012 | 2026-04-27 | Handoff `producer-to-publisher` rechazado en demo | Payload usaba `asset_url`/`channel` (singular). Schema exige `asset_path`/`channels` (array) + `format`. | Documentados los campos requeridos en `docs/DEMO_PIPELINE_RRSS.md` paso 3.6 y agregada plantilla en `templates/reels/cli-DEMO-rrss/5-errores-marketing-reel.template.json`. |
| L013 | 2026-04-27 | `edge-tts==6.1.12` rompio (`WSServerHandshakeError 403`) y luego SRT salio vacio | Microsoft cambio el handshake de Azure TTS; `SubMaker` solo recibia `WordBoundary`. | `requirements.txt -> edge-tts==7.2.8` y `synth.py` ahora alimenta `SubMaker` con `WordBoundary` **y** `SentenceBoundary`. |
| L014 | 2026-04-27 | `video-compose render` salia con codigo 0 sin generar nada tras `cmd_validate` | `set -e` + un `[ "$missing" -gt 0 ]` final que evaluaba a falso terminaba el script con exit 1 silencioso. | Se agrego `return 0` explicito al final de `cmd_validate` y se reescribio el filtergraph de audio para usar flags `has_voice`/`has_music` y mapear correctamente `[1:a]`/`[2:a]` segun lo que exista. |
| L015 | 2026-04-28 | Integrar 40 skills de marketingskills sin romper rutas ni ops existentes | Los enlaces `./skill/` del upstream son relativos a `skills/` plano; aqui cada SKILL vive en `skills/<name>/`. Enlaces `../../tools/` apuntaban al repo upstream. | Script `generate_marketing_skills.py`: rewrite `./foo/` -> `../foo/` (salvo `references/`), copia de `tools/` a `docs/upstream-marketingskills/tools/`, guía ES + cuerpo upstream; validador ignora rutas web `/foo`. Ops Jarvis no se sobrescriben. |
| L016 | 2026-04-28 | Importación masiva como **envoltura** (guía ES + cuerpo upstream EN sin adaptar) | Priorizar velocidad de import; skills `image`/`video` sin hooks al pipeline local; runtime sin sync de `agents/marketing/skills/`. | **Adaptación v2:** motor `marketing_skills_v2/` + manifiesto + YAML por skill; cuerpo ES; `references/upstream-en.md`; validador estricto; `sync-marketing-skills-from-repo.sh`; docs/SOUL/README actualizados. |
| L017 | 2026-04-28 | Benchmark **ClawWork** (HKUDS) promete “OpenClaw drop-in” pero integra **Nanobot** + GDPVal + stack pesado | Confundir benchmark económico con gobierno de cliente real; duplicar runtime | Forense `docs/CLAWWORK_FORENSE.md`; adoptar solo ideas: modos A/B/C/D (`AUTONOMIA_MODOS.md`), escalación async (`ESCALACION_ASYNC.md`), cost footer (`economic-accountability-ops`), auditoría previa (`llm-as-judge-ops`). Default modo **D** = sin cambio de comportamiento hasta que el CEO suba modo en `.env`. |

---
*Este archivo debe ser actualizado por cualquier agente que reciba una corrección directa del usuario.*
