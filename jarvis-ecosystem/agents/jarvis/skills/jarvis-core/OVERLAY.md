## Overlay clawvis — jarvis-core (holding OpenClaw)

Precedencia y honestidad de `jarvis-core` aplican. Ajustes holding:

1. **Aprobación CEO / AG gates** antes de acciones irreversibles (publish RRSS, deploy, cambios `openclaw.json`).
2. Orquestación por defecto: `jarvis-experts` → `fan-out-synthesize-ops` (no invertir).
3. Memoria: Engram MCP si disponible; si no, `MEMORY.md` + dossiers + Trello.
4. Skills de dominio holding (marketing×40, `proposal-ops`, `carousel-ops`) **no** se sustituyen por globales.

Ver: [APPROVAL_GATES.md](../../../docs/APPROVAL_GATES.md) y docs library `CLAWVIS_INTEGRATION.md`.
