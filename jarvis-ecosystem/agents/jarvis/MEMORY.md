# MEMORY.md - Long-term memory

Solo lectura/escritura en sesión principal con el humano (ver `AGENTS.md`).

## Trello (referencia rápida)

IDs útiles para la API (`curl` / skill **trello**). Actualiza con `GET /1/boards/{boardId}/lists` si añades listas.

| Recurso | Nombre | ID |
|---------|--------|-----|
| Tablero | Mi tablero de Trello | `69d0a352e4fed9476a5f6cec` |
| Lista | (rellenar) | — |

**Nota:** credenciales solo en `~/.openclaw/.env` (`TRELLO_API_KEY`, `TRELLO_TOKEN`), no en este archivo.
