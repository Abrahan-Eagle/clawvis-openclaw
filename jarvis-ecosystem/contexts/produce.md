# Context pack — PRODUCE

> Activar con `JARVIS_CONTEXT_MODE=produce` o *«modo produce»*.

## Priorizar

- `carousel-ops` / `carousel-render` / `video-compose` / `brand-kit`
- `copywriting-ops`, `de-ai-ify`, `creative-qa`
- `handoff` schemas (`copy-to-design`, `design-to-producer`, `producer-to-publisher`)
- `activity-log` start/event/end
- Modo autonomía **C** típico (produce; no publica)

## No hacer

- No publicar en redes (`mkt-publish` live / Meta)
- No saltarse `creative-qa` + `de-ai-ify` en assets visibles
- No inventar métricas

## Presupuesto de contexto

- 1 task_id + 1 dossier + brief corto + brand tokens
- Evitar pegar transcripts largos; usar paths a `out/`

## Pre-gate

Antes de AG-12: `judge-run` → luego `approval-gate request` (STOP hasta CEO).

## Compact

Fin de unidad de asset: `session-compact-ops`
